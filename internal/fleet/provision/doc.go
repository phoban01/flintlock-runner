// Package provision is the Fleet Controller's Provisioner: it runs the
// rendered provisioning scripts on one instance through fleet.Remote, in
// order, and reports each step (docs/requirements/06-fleet.md,
// host-provisioning).
package provision
