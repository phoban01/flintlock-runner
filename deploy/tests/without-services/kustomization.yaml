# Test overlay for `make manifests-check`: the fleet with every Host Service
# left out, which leaves the Exec Agent alone in the Host Agent.
#
#= docs/requirements/12-cluster-fleet.md#cluster-host-agent
#= type=test
#/ Where a Host Service is disabled in the configuration, the Host
#/ Agent SHALL NOT run it.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../host-agent
